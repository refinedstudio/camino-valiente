---
import "../styles/global.css";
import "@fontsource-variable/playfair-display";
import "@fontsource/lato/100.css";
import "@fontsource/lato/300.css";
import "@fontsource/lato/400.css";
import "@fontsource/lato/700.css";
import "@fontsource/lato/900.css";
---

<!doctype html>
<html>
  <head>
    <meta property="og:locale" content="es" />
    <meta name="twitter:card" content="summary_large_image" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, maximum-scale=5"
    />
    <meta name="theme-color" content="#ffffff" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://cdn.jsdelivr.net" />
    <title>Pagina no encontrada - Camino Valiente</title>
  </head>
  <body>
    <img loading="lazy"
      src="https://images.pexels.com/photos/28464001/pexels-photo-28464001/free-photo-of-impresionantes-vistas-de-las-dolomitas-en-canazei-italia.jpeg"
      class="w-screen h-screen absolute -z-10 left-0"
      alt="image"
    />
    <div class="w-screen h-screen bg-black/50 absolute z-0"></div>
    <main
      class="min-h-screen flex items-center justify-center p-8 overflow-hidden relative z-10"
    >
      <div></div>
      <div class="max-w-6xl w-full grid lg:grid-cols-2 gap-16 items-center">
        <!-- Contenido principal -->
        <div class="z-10">
          <h1
            class="font-serif text-3xl md:text-6xl font-bold text-white leading-tight uppercase tracking-wide"
          >
            TE PERDISTE EN EL
            <span class="uppercase text-white block">CAMINO</span>
          </h1>

          <p
            class="text-base text-white/70 leading-relaxed mt-4 font-normal mb-10"
          >
            Lo sentimos. No encontramos la página que buscas. Puede intentar
            buscar en nuestro sitio o regresar a casa.
          </p>
          <a
            href="/"
            class="inline-block bg-white text-neutral-950 px-10 py-4 font-semibold text-xs tracking-widest uppercase transition-all duration-300 hover:bg-white/5 backdrop-blur hover:-translate-y-1 hover:shadow-2xl hover:shadow-gray-800/20 hover:text-white"
          >
            VOLVER A CASA
          </a>
        </div>
      </div>
    </main>
  </body>
</html>

<style>
  /* Animaciones personalizadas que no están en Tailwind */
  @keyframes spin-slow {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }

  @keyframes bounce-slow {
    0%,
    20%,
    50%,
    80%,
    100% {
      transform: translateY(0);
    }
    40% {
      transform: translateY(-10px);
    }
    60% {
      transform: translateY(-5px);
    }
  }

  @keyframes float {
    0%,
    100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-10px);
    }
  }

  @keyframes float-reverse {
    0%,
    100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(10px);
    }
  }

  @keyframes compass-wiggle {
    0%,
    100% {
      transform: rotate(0deg);
    }
    25% {
      transform: rotate(15deg);
    }
    75% {
      transform: rotate(-15deg);
    }
  }

  @keyframes twinkle {
    from {
      opacity: 0.5;
      transform: scale(1);
    }
    to {
      opacity: 1;
      transform: scale(1.2);
    }
  }

  .animate-spin-slow {
    animation: spin-slow 20s linear infinite;
  }
  .animate-bounce-slow {
    animation: bounce-slow 2s ease-in-out infinite;
  }
  .animate-float {
    animation: float 3s ease-in-out infinite;
  }
  .animate-float-reverse {
    animation: float-reverse 2s ease-in-out infinite;
  }
  .animate-compass-wiggle {
    animation: compass-wiggle 4s ease-in-out infinite;
  }
  .animate-twinkle {
    animation: twinkle 2s ease-in-out infinite alternate;
  }
  .animate-twinkle-delay-1 {
    animation: twinkle 2s ease-in-out infinite alternate 0.5s;
  }
  .animate-twinkle-delay-2 {
    animation: twinkle 2s ease-in-out infinite alternate 1s;
  }
  .animate-twinkle-delay-3 {
    animation: twinkle 2s ease-in-out infinite alternate 1.5s;
  }

  /* Formas personalizadas para continentes */
  .rounded-custom-1 {
    border-radius: 20px 40px 30px 20px;
  }
  .rounded-custom-2 {
    border-radius: 15px 25px 20px 15px;
  }
  .rounded-custom-3 {
    border-radius: 25px 30px 15px 20px;
  }
</style>
